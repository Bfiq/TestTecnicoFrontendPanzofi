import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    midnightGreen: {
      main: '#083642',
    },
    gunmetal: {
      main: '#002B36',
    },
    sinopia: {
      main: '#CB4B16',
    },
    silver: {
      main: '#C1C2C3',
    },
    seasalt:{
        main: "#FAFAFA"
    },
    gray:{
      main: "#DCDCDC"
  }
  },
});

export {theme}
